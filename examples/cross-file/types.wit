package example:types@0.1.0;

interface types {
    record user {
        id: u64,
        name: string,
        email: string,
    }

    record post {
        id: u64,
        author: user,
        title: string,
    }

    enum status {
        active,
        inactive,
    }
}
